<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
        로그 레벨 (낮음 -> 높음)
        TRACE: 매우 상세한 추적 로그
        DEBUG: 디버깅을 위한 진단 로그
        INFO : 일반적인 애플리케이션 흐름
        WARN : 처리 가능한 예외/비정상 상황
        ERROR: 작업/요청을 막는 실패
    -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />

    <!-- Base file output -->
    <property name="LOG_PATH" value="logs" />
    <property name="LOG_FILE" value="${LOG_PATH}/mocktalkback.log" />

    <!-- Profile-specific noise control -->
    <springProfile name="dev">
        <logger name="org.springframework.web.servlet.resource" level="WARN" />
        <logger name="org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver" level="WARN" />
        <!-- <logger name="org.hibernate.SQL" level="WARN" />
        <logger name="org.hibernate.orm.jdbc.bind" level="WARN" /> -->
        <logger name="p6spy" level="INFO" />
    </springProfile>

    <springProfile name="prod">
        <logger name="org.springframework.web.servlet.resource" level="ERROR" />
        <logger name="org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver" level="ERROR" />
        <logger name="p6spy" level="OFF" />
    </springProfile>

    <!-- Daily rolling + size-based split; total size cap approximates "keep 10 files" -->
    <appender name="ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/mocktalkback-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1000MB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ROLLING" />
    </root>
</configuration>
